# Chat Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/chat` ページにチバ ↔ 蓮のリアルタイムチャット（テキスト + ファイル添付）を実装し Discord の代替とする。

**Architecture:** Supabase の `channels` + `messages` テーブルを追加し、SWR `refreshInterval: 3000` でポーリング更新する。サーバーコンポーネントが初期メッセージを取得、クライアントコンポーネントが差分を定期取得して楽観的更新で送信する。ファイルは Cloudflare R2 に保存し署名付き URL を messages に記録する。

**Tech Stack:** Next.js 15 App Router (Edge Runtime) / Supabase (Postgres) / SWR / shadcn/ui / Cloudflare R2

---

## ファイル構成

| 操作 | パス | 役割 |
|---|---|---|
| 新規作成 | `supabase/migrations/20260525000000_chat.sql` | channels + messages テーブル定義 |
| 新規作成 | `app/chat/types.ts` | Channel / Message 型定義 |
| 新規作成 | `app/api/chat/messages/route.ts` | GET（一覧）/ POST（投稿）|
| 新規作成 | `app/chat/page.tsx` | サーバーコンポーネント（初期データ取得）|
| 新規作成 | `app/chat/ChatRoom.tsx` | クライアントコンポーネント（ポーリング + 送信）|
| 新規作成 | `app/api/chat/upload/route.ts` | R2 ファイルアップロード |
| 変更 | `app/_components/Nav.tsx` | Chat リンクを追加 |
| 変更 | `app/page.tsx` | ホームに Chat クイックリンクを追加 |

---

## Task 1: DB マイグレーション

**Files:**
- Create: `supabase/migrations/20260525000000_chat.sql`

- [ ] **Step 1: マイグレーションファイルを作成する**

`supabase/migrations/20260525000000_chat.sql` を作成:

```sql
CREATE TABLE channels (
  id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name       TEXT NOT NULL UNIQUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

INSERT INTO channels (name) VALUES ('general');

CREATE TABLE messages (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  channel_id   UUID NOT NULL REFERENCES channels(id) ON DELETE CASCADE,
  sender_email TEXT NOT NULL,
  body         TEXT,
  file_url     TEXT,
  file_name    TEXT,
  file_size    BIGINT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT messages_has_content CHECK (body IS NOT NULL OR file_url IS NOT NULL)
);

CREATE INDEX idx_messages_channel ON messages(channel_id, created_at ASC);
```

- [ ] **Step 2: Supabase ダッシュボードで SQL を適用する**

Supabase ダッシュボード → SQL Editor → 上記 SQL を貼り付けて実行。

- [ ] **Step 3: テーブル作成を確認する**

SQL Editor で以下を実行し、2 行返ることを確認:

```sql
SELECT table_name FROM information_schema.tables
WHERE table_name IN ('channels', 'messages');
```

期待出力: `channels`, `messages` の 2 行

- [ ] **Step 4: channels に general が入っていることを確認する**

```sql
SELECT * FROM channels;
```

期待出力: name = 'general' の 1 行

- [ ] **Step 5: コミットする**

```bash
git add supabase/migrations/20260525000000_chat.sql
git commit -m "feat(chat): add channels and messages tables"
```

---

## Task 2: 型定義

**Files:**
- Create: `app/chat/types.ts`

- [ ] **Step 1: 型ファイルを作成する**

`app/chat/types.ts` を作成:

```typescript
export type Channel = {
  id: string;
  name: string;
  created_at: string;
};

export type Message = {
  id: string;
  channel_id: string;
  sender_email: string;
  body: string | null;
  file_url: string | null;
  file_name: string | null;
  file_size: number | null;
  created_at: string;
};
```

- [ ] **Step 2: コミットする**

```bash
git add app/chat/types.ts
git commit -m "feat(chat): add Channel and Message types"
```

---

## Task 3: Messages API（GET + POST）

**Files:**
- Create: `app/api/chat/messages/route.ts`

- [ ] **Step 1: API ルートを作成する**

`app/api/chat/messages/route.ts` を作成:

```typescript
import { getServiceClient } from '@/lib/supabase';
import { getCurrentEmail } from '@/lib/user';

export const runtime = 'edge';

export async function GET(req: Request) {
  const email = await getCurrentEmail().catch(() => null);
  if (!email) return new Response('Unauthorized', { status: 401 });

  const url = new URL(req.url);
  const channelId = url.searchParams.get('channel_id');
  if (!channelId) return new Response('channel_id required', { status: 400 });

  const db = getServiceClient();
  const { data, error } = await db
    .from('messages')
    .select('*')
    .eq('channel_id', channelId)
    .order('created_at', { ascending: true })
    .limit(50);

  if (error) return new Response(error.message, { status: 500 });
  return Response.json(data ?? []);
}

export async function POST(req: Request) {
  const email = await getCurrentEmail().catch(() => null);
  if (!email) return new Response('Unauthorized', { status: 401 });

  const { channel_id, body, file_url, file_name, file_size } = await req.json();
  if (!channel_id || (!body && !file_url)) {
    return new Response('channel_id and body or file required', { status: 400 });
  }

  const db = getServiceClient();
  const { data, error } = await db
    .from('messages')
    .insert({
      channel_id,
      sender_email: email,
      body: body || null,
      file_url: file_url || null,
      file_name: file_name || null,
      file_size: file_size || null,
    })
    .select('*')
    .single();

  if (error) return new Response(error.message, { status: 500 });
  return Response.json(data, { status: 201 });
}
```

- [ ] **Step 2: dev サーバーを起動して GET をテストする**

```bash
npm run dev
```

別ターミナルで `channels` テーブルの general の id を確認してから:

```bash
curl "http://localhost:3000/api/chat/messages?channel_id=<general-uuid>"
```

期待出力: `[]`（空配列）

- [ ] **Step 3: POST をテストする**

```bash
curl -X POST http://localhost:3000/api/chat/messages \
  -H "Content-Type: application/json" \
  -d '{"channel_id":"<general-uuid>","body":"test message"}'
```

期待出力: 201 + 作成された message オブジェクト（id, sender_email 等を含む）

- [ ] **Step 4: GET でメッセージが返ることを確認する**

```bash
curl "http://localhost:3000/api/chat/messages?channel_id=<general-uuid>"
```

期待出力: `[{"id":"...","body":"test message",...}]` の 1 件

- [ ] **Step 5: コミットする**

```bash
git add app/api/chat/messages/route.ts
git commit -m "feat(chat): add messages GET and POST API"
```

---

## Task 4: Chat ページ（サーバーコンポーネント + クライアント）

**Files:**
- Create: `app/chat/page.tsx`
- Create: `app/chat/ChatRoom.tsx`

- [ ] **Step 1: サーバーコンポーネントを作成する**

`app/chat/page.tsx` を作成:

```typescript
import { getServiceClient } from '@/lib/supabase';
import { getCurrentEmail } from '@/lib/user';
import { redirect } from 'next/navigation';
import ChatRoom from './ChatRoom';
import type { Channel, Message } from './types';

export const runtime = 'edge';

export default async function ChatPage() {
  const email = await getCurrentEmail().catch(() => null);
  if (!email) redirect('/');

  const db = getServiceClient();

  const { data: channels } = await db
    .from('channels')
    .select('*')
    .order('created_at', { ascending: true })
    .limit(10);

  const channel = channels?.[0] as Channel | undefined;
  if (!channel) {
    return (
      <main className="flex flex-1 items-center justify-center">
        <p className="text-muted-foreground text-sm">チャンネルがありません</p>
      </main>
    );
  }

  const { data: messages } = await db
    .from('messages')
    .select('*')
    .eq('channel_id', channel.id)
    .order('created_at', { ascending: true })
    .limit(50);

  return (
    <main className="flex flex-1 overflow-hidden">
      <ChatRoom
        channel={channel}
        initialMessages={(messages as Message[]) ?? []}
        currentEmail={email}
      />
    </main>
  );
}
```

- [ ] **Step 2: クライアントコンポーネントを作成する**

`app/chat/ChatRoom.tsx` を作成:

```typescript
'use client';

import { useState, useEffect, useRef, useCallback } from 'react';
import useSWR from 'swr';
import { toast } from 'sonner';
import { Button } from '@/components/ui/button';
import { Send } from 'lucide-react';
import type { Channel, Message } from './types';

const fetcher = (url: string): Promise<Message[]> =>
  fetch(url).then(r => {
    if (!r.ok) throw new Error('fetch failed');
    return r.json();
  });

export default function ChatRoom({
  channel,
  initialMessages,
  currentEmail,
}: {
  channel: Channel;
  initialMessages: Message[];
  currentEmail: string;
}) {
  const [allMessages, setAllMessages] = useState<Message[]>(initialMessages);
  const [body, setBody] = useState('');
  const [sending, setSending] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  const { data: polled } = useSWR<Message[]>(
    `/api/chat/messages?channel_id=${channel.id}`,
    fetcher,
    { refreshInterval: 3000, fallbackData: initialMessages }
  );

  useEffect(() => {
    if (!polled) return;
    setAllMessages(prev => {
      const existingIds = new Set(prev.map(m => m.id));
      const fresh = polled.filter(m => !existingIds.has(m.id));
      return fresh.length ? [...prev, ...fresh] : prev;
    });
  }, [polled]);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [allMessages]);

  const sendMessage = useCallback(async (e: React.FormEvent) => {
    e.preventDefault();
    const text = body.trim();
    if (!text || sending) return;
    setSending(true);
    try {
      const res = await fetch('/api/chat/messages', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ channel_id: channel.id, body: text }),
      });
      if (!res.ok) throw new Error('送信失敗');
      const msg: Message = await res.json();
      setAllMessages(prev => [...prev, msg]);
      setBody('');
    } catch {
      toast.error('送信に失敗しました');
    } finally {
      setSending(false);
    }
  }, [body, sending, channel.id]);

  const handleKeyDown = useCallback((e: React.KeyboardEvent<HTMLTextAreaElement>) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage(e as unknown as React.FormEvent);
    }
  }, [sendMessage]);

  return (
    <div className="flex flex-col flex-1 h-full min-h-0">
      {/* チャンネルヘッダー */}
      <div className="border-b px-4 py-2.5 flex items-center gap-2 shrink-0">
        <span className="text-sm font-semibold text-muted-foreground">#</span>
        <span className="text-sm font-semibold">{channel.name}</span>
      </div>

      {/* メッセージ一覧 */}
      <div className="flex-1 overflow-y-auto p-4 space-y-3 min-h-0">
        {allMessages.length === 0 && (
          <p className="text-center text-sm text-muted-foreground py-12">
            まだメッセージがありません。最初のメッセージを送ってみましょう！
          </p>
        )}
        {allMessages.map(msg => {
          const isMe = msg.sender_email === currentEmail;
          return (
            <div key={msg.id} className={`flex gap-2 ${isMe ? 'flex-row-reverse' : ''}`}>
              <div
                className={`max-w-[70%] rounded-2xl px-3 py-2 text-sm ${
                  isMe
                    ? 'bg-primary text-primary-foreground rounded-tr-sm'
                    : 'bg-secondary text-secondary-foreground rounded-tl-sm'
                }`}
              >
                {!isMe && (
                  <p className="text-xs font-medium mb-0.5 opacity-60">
                    {msg.sender_email.split('@')[0]}
                  </p>
                )}
                {msg.body && <p className="whitespace-pre-wrap break-words">{msg.body}</p>}
                {msg.file_url && (
                  <a
                    href={msg.file_url}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="underline text-xs mt-1 block"
                  >
                    📎 {msg.file_name ?? 'ファイル'}
                    {msg.file_size && ` (${(msg.file_size / 1024 / 1024).toFixed(1)} MB)`}
                  </a>
                )}
              </div>
            </div>
          );
        })}
        <div ref={bottomRef} />
      </div>

      {/* 入力欄 */}
      <form onSubmit={sendMessage} className="border-t p-3 flex gap-2 items-end shrink-0">
        <textarea
          className="flex-1 resize-none rounded-md border border-input bg-background px-3 py-2 text-sm outline-none focus:ring-1 focus:ring-ring min-h-[40px] max-h-[120px]"
          placeholder="メッセージを入力… (Enter で送信 / Shift+Enter で改行)"
          value={body}
          onChange={e => setBody(e.target.value)}
          onKeyDown={handleKeyDown}
          disabled={sending}
          rows={1}
        />
        <Button
          type="submit"
          disabled={!body.trim() || sending}
          size="sm"
          className="h-10 px-3"
        >
          <Send className="h-4 w-4" />
        </Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: ブラウザで動作確認する**

`http://localhost:3000/chat` を開く。  
期待: 「まだメッセージがありません」が表示される。

- [ ] **Step 4: メッセージ送信を確認する**

テキストを入力して Enter または送信ボタンを押す。  
期待: メッセージが一覧に追加される（画面右側に表示）。

- [ ] **Step 5: ポーリングを確認する**

別ブラウザ（or シークレットモード）で同じ URL を開く。片方でメッセージを送り、もう片方で 3 秒以内に表示されることを確認する。

- [ ] **Step 6: コミットする**

```bash
git add app/chat/page.tsx app/chat/ChatRoom.tsx
git commit -m "feat(chat): add chat page with polling-based realtime"
```

---

## Task 5: Nav + ホームページ更新

**Files:**
- Modify: `app/_components/Nav.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: Nav に Chat リンクを追加する**

`app/_components/Nav.tsx` の `links` 配列を更新:

```typescript
const links = [
  { href: '/chat', label: 'Chat' },
  { href: '/morning', label: 'Morning' },
  { href: '/daily-reports', label: '日報' },
  { href: '/notes', label: 'ノート' },
];
```

- [ ] **Step 2: ホームの quickLinks に Chat を追加する**

`app/page.tsx` の `quickLinks` 配列を更新:

```typescript
import { MessageSquare, Newspaper, BookOpen, Sun } from 'lucide-react';

const quickLinks = [
  { href: '/chat', label: 'Chat', desc: '蓮とメッセージ', icon: MessageSquare },
  { href: '/morning', label: 'Morning Feed', desc: 'AI ニュースを確認', icon: Sun },
  { href: '/daily-reports', label: '日報', desc: '今日の進捗を記録', icon: Newspaper },
  { href: '/notes', label: 'ノート', desc: 'アイデアをメモ', icon: BookOpen },
];
```

`grid-cols-1 sm:grid-cols-3` を `grid-cols-2 sm:grid-cols-4` に変更（4 枠になるため）:

```typescript
<div className="grid grid-cols-2 sm:grid-cols-4 gap-4 w-full max-w-2xl">
```

`max-w-xl` も `max-w-2xl` に変更（上記の通り）。

- [ ] **Step 3: ブラウザで確認する**

- `http://localhost:3000` を開き、Chat カードが表示されることを確認
- Nav に「Chat」リンクが追加されていることを確認
- Chat リンクをクリックして `/chat` に遷移することを確認

- [ ] **Step 4: コミットする**

```bash
git add app/_components/Nav.tsx app/page.tsx
git commit -m "feat(chat): add chat link to nav and home"
```

---

## Task 6: R2 ファイルアップロード

> **注意:** このタスクは Cloudflare ダッシュボードの手動操作が必要です。後回しにして先にデプロイしても問題ありません。

**Files:**
- Create: `app/api/chat/upload/route.ts`

- [ ] **Step 1: R2 バケットを作成する（手動・CF ダッシュボード）**

1. Cloudflare ダッシュボード → R2 → バケットを作成 → 名前: `synapse-files`
2. Pages プロジェクト → Settings → Functions → R2 bindings → 追加
   - Variable name: `FILES_BUCKET`
   - R2 bucket: `synapse-files`
3. `.dev.vars.example` に `# FILES_BUCKET = (R2 binding - no value needed locally)` を追記（ローカルでは R2 は使わない）

- [ ] **Step 2: アップロード API を作成する**

`app/api/chat/upload/route.ts` を作成:

```typescript
import { getCurrentEmail } from '@/lib/user';
import { getRequestContext } from '@cloudflare/next-on-pages';

export const runtime = 'edge';

const MAX_SIZE = 10 * 1024 * 1024; // 10 MB

export async function POST(req: Request) {
  const email = await getCurrentEmail().catch(() => null);
  if (!email) return new Response('Unauthorized', { status: 401 });

  const formData = await req.formData();
  const file = formData.get('file') as File | null;
  if (!file) return new Response('file required', { status: 400 });
  if (file.size > MAX_SIZE) return new Response('File too large (max 10 MB)', { status: 413 });

  const env = getRequestContext().env as { FILES_BUCKET?: R2Bucket };
  const bucket = env.FILES_BUCKET;
  if (!bucket) return new Response('R2 not configured', { status: 503 });

  const key = `chat/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, '_')}`;
  await bucket.put(key, await file.arrayBuffer(), {
    httpMetadata: { contentType: file.type },
  });

  // 署名付き URL の代わりに公開 URL を返す
  // R2 バケットを Public にする場合: https://<account>.r2.cloudflarestorage.com/<bucket>/<key>
  // バケット非公開の場合は signed URL が必要（将来対応）
  const fileUrl = `https://pub-<your-r2-public-domain>.r2.dev/${key}`;

  return Response.json({
    file_url: fileUrl,
    file_name: file.name,
    file_size: file.size,
  });
}
```

> `<your-r2-public-domain>` は R2 バケットの Public URL 設定後に実際の値に変更すること。

- [ ] **Step 3: ChatRoom にファイル添付ボタンを追加する**

`app/chat/ChatRoom.tsx` の form 内、textarea の前に追加:

```typescript
// import に追加
import { Paperclip } from 'lucide-react';

// state に追加
const [uploading, setUploading] = useState(false);
const fileInputRef = useRef<HTMLInputElement>(null);

// handleFileChange 関数を追加（sendMessage の前に）
const handleFileChange = useCallback(async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (!file) return;
  setUploading(true);
  try {
    const fd = new FormData();
    fd.append('file', file);
    const res = await fetch('/api/chat/upload', { method: 'POST', body: fd });
    if (!res.ok) throw new Error('upload failed');
    const { file_url, file_name, file_size } = await res.json();
    const msgRes = await fetch('/api/chat/messages', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ channel_id: channel.id, file_url, file_name, file_size }),
    });
    if (!msgRes.ok) throw new Error('message failed');
    const msg: Message = await msgRes.json();
    setAllMessages(prev => [...prev, msg]);
  } catch {
    toast.error('ファイルの送信に失敗しました');
  } finally {
    setUploading(false);
    if (fileInputRef.current) fileInputRef.current.value = '';
  }
}, [channel.id]);

// form 内の textarea の前に追加
<input
  ref={fileInputRef}
  type="file"
  className="hidden"
  onChange={handleFileChange}
/>
<Button
  type="button"
  variant="ghost"
  size="sm"
  className="h-10 px-2 shrink-0"
  onClick={() => fileInputRef.current?.click()}
  disabled={uploading || sending}
>
  <Paperclip className="h-4 w-4" />
</Button>
```

- [ ] **Step 4: コミットする**

```bash
git add app/api/chat/upload/route.ts app/chat/ChatRoom.tsx
git commit -m "feat(chat): add R2 file upload"
```

---

## Task 7: デプロイ + 本番確認

- [ ] **Step 1: 型エラーがないことを確認する**

```bash
npx tsc --noEmit
```

期待: エラーなし

- [ ] **Step 2: master に push してデプロイする**

```bash
git push origin master
```

CF Pages が自動ビルド・デプロイする（約 2〜3 分）。

- [ ] **Step 3: 本番で動作確認する**

`https://synapse-9dv.pages.dev/chat` を開く。
- [ ] メッセージ送信が動作する
- [ ] 3 秒以内に別ブラウザでメッセージが反映される
- [ ] Nav に Chat リンクが表示される
- [ ] ホームに Chat カードが表示される

---

## スコープ外（次のプラン）

- **Phase 2**: ニュース精度改善（ソース刷新・フィルタ・sandbox パス）
- 未読バッジ（`last_read_at` 追跡）
- Web Push 通知
- チャンネル複数対応
